# Backend API URL (required for Vercel + Railway deployment)
# When empty, uses relative URLs (works with Vite dev proxy or nginx)
# Example: https://your-app.up.railway.app
VITE_API_URL=

# WebSocket URL (defaults to VITE_API_URL with http→ws)
# Example: wss://your-app.up.railway.app
VITE_WS_URL=
